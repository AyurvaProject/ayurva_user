import { Typography } from "@mui/material";

const Profile = () => {
  return (
    <div>
      <Typography variant="h4">My Profile</Typography>
      <Typography variant="body1">User details and medical history.</Typography>
    </div>
  );
};

export default Profile;
